# Autodetect text files
* text=auto

# Force the following filetypes to have unix eols, so Windows does not break them
*.* text eol=lf

# But do not process images.
*.png binary
*.jpg binary
*.jpeg binary
*.png binary
*.gif binary
*.ico binary
